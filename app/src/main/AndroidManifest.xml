<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- solicitando permissão para ter o app ter acesso a internet -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Debts"
        tools:targetApi="31">
        <activity
            android:name=".Dialog_Style.layout_AvisoExclusaoConta"
            android:exported="false" />
        <activity
            android:name=".configConta_Usuario"
            android:exported="false" />
        <activity
            android:name=".layout_Item_lista.ItemLista"
            android:exported="false" />
        <activity
            android:name=".tela_RelatorioGastos"
            android:exported="false" />
        <activity
            android:name=".telaPerfilUsuario"
            android:exported="false" />
        <activity
            android:name=".telaPrincipal"
            android:exported="false" />
        <activity
            android:name=".cadastrar"
            android:exported="false" />
        <activity
            android:name=".criarConta"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>